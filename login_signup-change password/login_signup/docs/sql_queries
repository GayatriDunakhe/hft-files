-- "sys" is a db name
USE sys;

-- creating table called user3
CREATE TABLE user3 (
    id INT AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(255) NOT NULL,
    password VARCHAR(255) CHARACTER SET utf8 COLLATE utf8_bin NOT NULL
);

select * from user3;